# ThreatIQ runtime dependencies.
#
# This file is deliberately lean. An earlier version pulled in torch and
# torch-geometric (about 2 GB together, loaded into RAM at import time), which
# exhausted the memory limit on our deployment platform. The container
# crash-looped until the platform removed the deployment entirely.
#
# Every package listed here is actually imported somewhere in src/. Packages
# that were previously declared but imported nowhere have been removed:
# matplotlib, seaborn, plotly, faker, networkx, tqdm, rich, aiofiles,
# asyncio-throttle, python-dateutil, pyyaml, pydantic-settings, ollama, groq
# and google-generativeai.

# --- Core numerics and models ---
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# --- Explainability (SHAP, with a built-in permutation fallback) ---
shap>=0.44.0

# --- API layer ---
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
websockets>=11.0.0

# --- ISO 20022 / ISO 8583 message formatting ---
lxml>=4.9.0
dicttoxml>=1.7.0

# --- Testing ---
pytest>=7.4.0
httpx>=0.24.0

# --- Optional: deep-learning extras -----------------------------------------
# torch and torch-geometric are NOT required to run ThreatIQ.
#
#   * The graph neural network (TransactionGNN) is not trained and is disabled
#     at serving time, so it contributes nothing to any published metric.
#   * The reinforcement-learning attackers fall back to a NumPy implementation
#     of REINFORCE with a learned baseline. It genuinely learns, and it is
#     covered by exactly the same tests as the PyTorch path.
#
# Install them only for local research on the GNN, or for PPO-based training:
#
#   pip install "torch>=2.0.0" "torch-geometric>=2.4.0"
#
# Note that doing so adds roughly 2 GB and will exceed the memory limit of most
# free hosting tiers.
